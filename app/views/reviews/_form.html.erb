<%= simple_form_for(
  [restaurant, review],
  html: { data: { action: "submit->reviews#create" } }
  ) do |f| %>
  <%= f.input :content, as: :text, label: false %>
  <%= f.submit 'Submit' %>
<% end %>
